清渊 v4.3
================================

修复内容：

1. 浏览器误识别：
   浏览器 -> 楼览器 / 游览器 / 瀏覽器
   仍会路由到 browser_search，
   不再错误进入通用 GUI 模式。

2. 确认词误识别：
   同意 -> 同一 / 统一 / 統一
   仍然视为明确同意。

3. 任务执行中不会显示：
   🌙 清渊休息啦。
   只有真正空闲 45 秒才显示一次。

4. 用户已经明确下达操作命令时，
   清渊不能再次口头问“是否需要我执行”，
   应直接进入 Task Permit。

5. 所有 README/版本说明统一放入：
   C:\MyAgent\docs\
